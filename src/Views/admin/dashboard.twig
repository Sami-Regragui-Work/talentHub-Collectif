{% extends "layout.twig" %}

{% block title %}Tableau de bord - Admin{% endblock %}

{% block navigation %}
    <a href="/admin/dashboard" class="px-3 py-2 rounded-md text-white bg-gray-700">Tableau de bord</a>
    <a href="/admin/categories" class="px-3 py-2 rounded-md text-gray-300 hover:bg-gray-700">Catégories</a>
    <a href="/admin/tags" class="px-3 py-2 rounded-md text-gray-300 hover:bg-gray-700">Tags</a>
    <a href="/admin/offers" class="px-3 py-2 rounded-md text-gray-300 hover:bg-gray-700">Toutes les offres</a>
    <a href="/admin/recruiters" class="px-3 py-2 rounded-md text-gray-300 hover:bg-gray-700">Recruteurs</a>
    <a href="/admin/candidates" class="px-3 py-2 rounded-md text-gray-300 hover:bg-gray-700">Candidats</a>
{% endblock %}

{% block content %}
<div class="px-4 py-6 sm:px-0">
    <h2 class="text-3xl font-bold text-white">Bienvenue, {{ user.fullName }}</h2>
    <p class="mt-2 text-gray-400">Vue d'ensemble de la plateforme</p>
</div>

<!-- Statistics Cards -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
    <div class="bg-gray-800 overflow-hidden shadow-lg rounded-lg border border-gray-700">
        <div class="p-6">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-secondary/10 rounded-lg flex items-center justify-center">
                        <span class="text-2xl">📁</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-3xl font-semibold text-white">{{ stats.categories|default('0') }}</p>
                    <p class="text-gray-400 text-sm">Catégories</p>
                </div>
            </div>
        </div>
    </div>

    <div class="bg-gray-800 overflow-hidden shadow-lg rounded-lg border border-gray-700">
        <div class="p-6">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-success/10 rounded-lg flex items-center justify-center">
                        <span class="text-2xl">🏷️</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-3xl font-semibold text-white">{{ stats.tags|default('0') }}</p>
                    <p class="text-gray-400 text-sm">Tags</p>
                </div>
            </div>
        </div>
    </div>

    <div class="bg-gray-800 overflow-hidden shadow-lg rounded-lg border border-gray-700">
        <div class="p-6">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-warning/10 rounded-lg flex items-center justify-center">
                        <span class="text-2xl">👔</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-3xl font-semibold text-white">{{ stats.recruiters|default('0') }}</p>
                    <p class="text-gray-400 text-sm">Recruteurs</p>
                </div>
            </div>
        </div>
    </div>

    <div class="bg-gray-800 overflow-hidden shadow-lg rounded-lg border border-gray-700">
        <div class="p-6">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-secondary/10 rounded-lg flex items-center justify-center">
                        <span class="text-2xl">💼</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-3xl font-semibold text-white">{{ stats.offers|default('0') }}</p>
                    <p class="text-gray-400 text-sm">Offres actives</p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="bg-gray-800 shadow-lg rounded-lg p-6 border border-gray-700">
    <h3 class="text-lg font-semibold text-white mb-4">Actions rapides</h3>
    <div class="flex flex-wrap gap-3">
        <a href="/admin/categories/create" class="inline-flex items-center px-4 py-2 bg-secondary hover:bg-blue-600 text-white rounded-md">
            + Nouvelle catégorie
        </a>
        <a href="/admin/tags/create" class="inline-flex items-center px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white rounded-md">
            + Nouveau tag
        </a>
        <a href="/admin/recruiters/create" class="inline-flex items-center px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white rounded-md">
            + Ajouter recruteur
        </a>
    </div>
</div>
{% endblock %}