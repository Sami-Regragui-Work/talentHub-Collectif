{% extends "layout.twig" %}

{% block title %}Tags - Admin{% endblock %}

{% block content %}
<div class="px-4 py-6 sm:px-0">
    <div class="flex justify-between items-center mb-6">
        <h2 class="text-3xl font-bold text-white">Tags</h2>
        <a href="/admin/tags/create" class="px-4 py-2 bg-secondary hover:bg-blue-600 text-white rounded-md">
            + Nouveau tag
        </a>
    </div>

    <div class="bg-gray-800 shadow-lg rounded-lg overflow-hidden border border-gray-700">
        <table class="min-w-full divide-y divide-gray-700">
            <thead class="bg-gray-700">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase">Nom</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase">Actions</th>
                </tr>
            </thead>
            <tbody class="bg-gray-800 divide-y divide-gray-700">
                {% for tag in tags %}
                <tr class="hover:bg-gray-700">
                    <td class="px-6 py-4 text-sm font-medium text-white">{{ tag.name }}</td>
                    <td class="px-6 py-4 space-x-2 text-sm">
                        <a href="/admin/tags/edit/{{ tag.name }}" class="text-warning hover:text-yellow-400">✏️</a>
                        <a href="/admin/tags/delete/{{ tag.name }}" class="text-danger hover:text-red-400" onclick="return confirm('Supprimer ce tag ?')">🗑️</a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}