{% extends "layout.twig" %}

{% block title %}Offres d'emploi{% endblock %}

{% block content %}
<div class="mb-8">
    <h1 class="text-4xl font-bold text-white text-center mb-4">Offres d'emploi</h1>
    <form method="GET" action="/jobs" class="max-w-2xl mx-auto flex gap-2">
        <input type="text" name="search" placeholder="Rechercher un poste..." 
               class="flex-1 px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:border-secondary">
        <button type="submit" class="px-6 py-2 bg-secondary text-white rounded-lg hover:bg-blue-600">
            🔍 Rechercher
        </button>
    </form>
</div>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {% for job in jobs %}
    <div class="bg-gray-800 shadow-lg rounded-lg p-6 border border-gray-700 hover:border-gray-600 transition-all">
        <div class="flex items-start justify-between mb-4">
            <h3 class="text-xl font-semibold text-white">{{ job.title }}</h3>
            <span class="text-2xl">💼</span>
        </div>
        <p class="text-gray-300 mb-2">{{ job.companyName }}</p>
        <p class="text-success font-semibold mb-2">💰 {{ job.salary ? job.salary ~ ' €' : 'Salaire non précisé' }}</p>
        <p class="text-gray-400 mb-4">📍 {{ job.location|default('Lieu non précisé') }}</p>
        <div class="mb-4">
            {% for tag in job.tags %}
            <span class="inline-block px-2 py-1 bg-gray-700 text-sm rounded mr-1">{{ tag }}</span>
            {% endfor %}
        </div>
        <a href="/jobs/{{ job.id }}" class="block w-full text-center px-4 py-2 bg-gray-700 hover:bg-secondary text-white rounded-md transition-colors">
            Voir l'offre →
        </a>
    </div>
    {% endfor %}
</div>
{% endblock %}